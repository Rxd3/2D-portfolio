{
  "name": "proto2d",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prune-dist.mjs",
    "preview": "vite preview",
    "check": "npm run build"
  },
  "devDependencies": {
    "terser": "^5.48.0",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "kaboom": "^3000.1.17"
  }
}
